import React, {FC} from 'react';
import styles from './ProfileCenter.module.scss';

const ProfileCenter : FC = (props) => {
    return (
        <div className={styles.profileCenter}></div>
    );
}

export default ProfileCenter;